/*
function calcular_distancia(x1,x2,y1,y2){
    return(math.qrts(math.pow((x2-x1),2)+math.pow((y2-y1),2)))
}

let distacia1=calcular_distancia(0,20,0,-50)
console.log(`la distacia calculada es ${distancia1.tofixed(2)} UA`)
*/

function calcular_distancia(x1,x2,y1,y2)=>(math.qrts(math.pow((x2-x1),2)+math.pow((y2-y1),2)))